[project]
name = "bank-statement-analyzer"
version = "0.1.0"
description = "Agentic bank statement analyzer for ITR filing (Indian CA firm)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
